---
hosts: all
become: yes 
gather_facts: yes 
tasks: 
- name: i want to install websrever on ubuntu nodes
  apt: 
    name: apache 2
    state: present 
- name: i want to start webserver on ubuntu nodes
  service: 
     name: apache2
     start: 